
Ai solicitat resetarea parolei pentru contul tău {Config::SITE_NAME}.

Nume de cont: {$user.username}
Adresa ta de e-mail: {$user.email}
Numele tău: {$user.full_name}

Pentru a confirma resetarea parolei, vizitează acest link:

----
{$clink}
----

Dacă nu ai făcut o astfel de solicitare, ignoră acest mesaj, iar parola nu va fi resetată.

Echipa {Config::SITE_NAME}
{Config::URL_HOST}{Config::URL_PREFIX}
